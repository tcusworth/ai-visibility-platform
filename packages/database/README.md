# Database

The production persistence layer will use PostgreSQL.

Core records include workspaces, targets, competitors, prompt-set versions, benchmark definitions, benchmark runs, observations, observation sources, authority results/evidence, metric snapshots, diagnostics, and recommended actions.

Google Sheets is an integration/import-export surface, not the platform database.
